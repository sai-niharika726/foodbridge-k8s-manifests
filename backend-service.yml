apiVersion: v1
kind: Service
metadata:
  namespace: foodbridge
  name: backend-service
spec:
  selector:
    app: backend
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 32000

apiVersion: v1
kind: Service
metadata:
  namespace: foodbridge
  name: frontend-service
spec:
  selector:
    app: frontend
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000

